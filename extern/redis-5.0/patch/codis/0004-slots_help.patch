diff --git a/extern/redis-5.0/src/help.h b/extern/redis-5.0/src/help.h
index 184d7672..ddf7f122 100644
--- a/extern/redis-5.0/src/help.h
+++ b/extern/redis-5.0/src/help.h
@@ -3,6 +3,9 @@
 #ifndef __REDIS_HELP_H
 #define __REDIS_HELP_H
 
+/* Notice!!!
+ * We can only append the new group name at the end of array, as the index of
+ * each group has been used in commandHelp array. */
 static char *commandGroups[] = {
     "generic",
     "string",
@@ -18,7 +21,8 @@ static char *commandGroups[] = {
     "hyperloglog",
     "cluster",
     "geo",
-    "stream"
+    "stream",
+    "codis"
 };
 
 struct commandHelp {
@@ -1162,7 +1166,157 @@ struct commandHelp {
     "destination numkeys key [key ...] [WEIGHTS weight] [AGGREGATE SUM|MIN|MAX]",
     "Add multiple sorted sets and store the resulting sorted set in a new key",
     4,
-    "2.0.0" }
+    "2.0.0" },
+    {"SLOTSHASHKEY",
+    "key [key...]",
+    "Show the slots num of the given keys",
+    15,
+    "codis1.9"},
+    {"SLOTSINFO",
+    "[start] [count]",
+    "Show the count of keys which in the specified slots",
+    15,
+    "codis1.9"},
+    {"SLOTSMGRTSLOT",
+    "host port timeout slot",
+    "Randomly pick a key from the specified slot and migrate it to the specified server",
+    15,
+    "codis1.9"},
+    {"SLOTSMGRTONE",
+    "host port timeout key",
+    "Migrate the specified key to the specified server",
+    15,
+    "codis1.9"},
+    {"SLOTSMGRTTAGSLOT",
+    "host port timeout slot",
+    "Randomly pick a key from the specified slot, and try to pick all the keys which have the same tag with the random one, then migrate them to the specified server.",
+    15,
+    "codis1.9"},
+    {"SLOTSMGRTTAGONE",
+    "host port timeout key",
+    "Try to pick all the keys which have the same tag with the specified one, then migrate them to the specified server.",
+    15,
+    "codis1.9"},
+    {"SLOTSRESTORE",
+    "key ttl val [key ttl val ...]",
+    "Restore all the key-value-ttl pairs to the current server",
+    15,
+    "codis1.9"},
+    {"SLOTSDEL",
+    "slot [slot ...]",
+    "Delete all the key-value pairs which in the specified slots",
+    15,
+    "codis1.9"},
+    {"SLOTSCHECK",
+    "-",
+    "Check the consistency between the redis db dict and codis hash slots",
+    15,
+    "codis1.9"},
+    {"SLOTSSCAN",
+    "slot cursor [COUNT count]",
+    "Iterate the set of keys which in the specified slot.",
+    15,
+    "codis3.1"},
+    {"SLOTSMGRTONE-ASYNC-DUMP",
+    "timeout maxbulks key [key...]",
+    "Serialize the values which stored at the given keys in RDB format and return it to the user for async migration.",
+    15,
+    "codis3.2"},
+    {"SLOTSMGRTTAGONE-ASYNC-DUMP",
+    "timeout maxbulks key [key...]",
+    "Try to pick all the keys which have the same tag with the given ones, serialize the values which stored at these keys in RDB format and retrn it to the user for async migration.",
+    15,
+    "codis3.2"},
+    {"SLOTSMGRTSLOT-ASYNC",
+    "host port timeout maxbulks maxbytes slot numkeys",
+    "Randomly pick several keys from the specified slot and migrate them to the specified server asynchronously.",
+    15,
+    "codis3.2"},
+    {"SLOTSMGRTTAGSLOT-ASYNC",
+    "host port timeout maxbulks maxbytes slot numkeys",
+    "Randomly pick several keys from the specified slot, find all the keys which have the same tag with the random ones, then migrate them to the specified server asynchronously.",
+    15,
+    "codis3.2"},
+    {"SLOTSMGRTONE-ASYNC",
+    "host port timeout maxbulks maxbytes key [key...]",
+    "Migrate the given keys to the specified server asynchronously.",
+    15,
+    "codis3.2"},
+    {"SLOTSMGRTTAGONE-ASYNC",
+    "host port timeout maxbulks maxbytes key [key...]",
+    "Pick all the keys which have the same tag with the given ones and migrate them to the specified server asynchronously.",
+    15,
+    "codis3.2"},
+    {"SLOTSMGRT-ASYNC-FENCE",
+    "-",
+    "Get the status of async migration from the client or block it.",
+    15,
+    "codis3.2"},
+    {"SLOTSMGRT-ASYNC-CANCEL",
+    "-",
+    "Cancel the ongoing async migration",
+    15,
+    "codis3.2"},
+    {"SLOTSMGRT-ASYNC-STATUS",
+    "-",
+    "Get the status and statistics about the ongoing async migration",
+    15,
+    "codis3.2"},
+    {"SLOTSMGRT-EXEC-WRAPPER",
+    "hashkey command [arg ...]",
+    "Execute the command for a key which is in the migrating slot",
+    15,
+    "codis3.2"},
+    {"SLOTSRESTORE-ASYNC DELETE",
+    "key",
+    "Delete the specified key on the target server before migrate it",
+    15,
+    "codis3.2"},
+    {"SLOTSRESTORE-ASYNC EXPIRE",
+    "key ttl",
+    "Change the ttl of the specified migrating key on the target server",
+    15,
+    "codis3.2"},
+    {"SLOTSRESTORE-ASYNC OBJECT",
+    "key ttl payload",
+    "Restore the given payload which is in RDB format to the specified key on the target server asynchronously.",
+    15,
+    "codis3.2"},
+    {"SLOTSRESTORE-ASYNC LIST",
+    "key ttl hint [elem ...]",
+    "Restore the given elemments to the specified list type key on the target server asynchronously.",
+    15,
+    "codis3.2"},
+    {"SLOTSRESTORE-ASYNC HASH",
+    "key ttl hint [field value ...]",
+    "Restore the given field-value pairs to the specified hash type key on the target server asynchronously.",
+    15,
+    "codis3.2"},
+    {"SLOTSRESTORE-ASYNC DICT",
+    "key ttl hint [elem ...]",
+    "Restore the given elemments to the specified set type key on the target server asynchronously.",
+    15,
+    "codis3.2"},
+    {"SLOTSRESTORE-ASYNC ZSET",
+    "key ttl hint [mem score ...]",
+    "Restore the given member-score pairs to the specified zset type key on the target server asynchronously.",
+    15,
+    "codis3.2"},
+    {"SLOTSRESTORE-ASYNC-ACK",
+    "errno message",
+    "Return the result of a SLOTSRESTORE-ASYNC command to the source server",
+    15,
+    "codis3.2"},
+    {"SLOTSRESTORE-ASYNC-AUTH",
+    "password",
+    "Authenticate to the target server for async migration",
+    15,
+    "codis3.2"},
+    {"SLOTSRESTORE-ASYNC-SELECT",
+    "db",
+    "Specify the database that the later keys will be restored to",
+    15,
+    "codis3.2"}
 };
 
 #endif
